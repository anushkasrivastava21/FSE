// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

// TODO (Person B — Phase 1):
// Soulbound ERC-721 — _beforeTokenTransfer reverts unless mint/burn.
// mint(donor, matchId, metadataURI) called only by Settlement.sol on delivery.
// See FSE_TRD.md §3.1 for full spec.

contract FoodCreditToken {
    // placeholder — Person B implements
}
